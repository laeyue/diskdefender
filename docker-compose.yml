version: '3.8'
services:
  disk-defender:
    build: .
    image: disk-defender:latest
    container_name: disk-defender
    ports:
      - "3000:3000"
    restart: unless-stopped
    environment:
      - PORT=3000
